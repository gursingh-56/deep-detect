# Leave blank to use the Vite dev-server proxy (recommended for local work).
# Set to the deployed Django origin for a production build, e.g.
# VITE_API_BASE_URL=https://deepdetect-api.example.com
VITE_API_BASE_URL=
